<%# CSS・・・assets/stylesheets/shared/header.css %>
<header class='top-page-header'>
  <div class='search-bar-contents'>
    <p><%= link_to image_tag("icon.png", class:"icon"), "/" %>ReadersHub</p>
      <%= form_with(url: search_books_path, local: true, method: :get, class: "search-form", action: "#") do |form| %>
      <%= form.text_field :keyword, class: 'input-box', placeholder: '本のタイトルで検索' %>
      <button class="search-button">
        <%= image_tag "search.png", class:"search-icon" %>
      </button>
    <% end %>
    </form>
  </div>
  <div class='nav'>
    <ul class='lists-left'>
    </ul>
    <ul class='lists-right'>
      <% if user_signed_in? %>
        <li><%= link_to current_user.nickname, "/users/#{current_user.id}", class: "user-nickname" %></li> 
        <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "logout" %></li> 
      <% else %>
        <li><%= link_to 'ログイン', new_user_session_path, class: "login" %></li>
        <li><%= link_to '新規登録', new_user_registration_path, class: "sign-up" %></li>
      <% end %>
    </ul>
  </div>
</header>